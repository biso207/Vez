// defines home screen filter models.

// defines the target audience used when inviting guests.
enum GuestAudienceFilter { friends, following, anyone }

// defines the rsvp status filters used by guest lists.
enum GuestStateFilter { all, going, notGoing, maybe }
